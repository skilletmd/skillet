{
  "name": "@skillet/mcp",
  "version": "0.1.0",
  "description": "Skillet MCP server transport \u2014 expose kit skills to any MCP-capable client",
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "pnpm --filter \"@skillet/mcp^...\" build && vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@skillet/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.2.6"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "license": "Apache-2.0"
}
